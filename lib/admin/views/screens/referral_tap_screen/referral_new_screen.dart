import 'package:flutter/material.dart';

class ReferralNewScreen extends StatelessWidget {
  const ReferralNewScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Referral New Screen')),);
  }
}
