import 'package:flutter/material.dart';

class AgentActiveScreen extends StatelessWidget {
  const AgentActiveScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Agent Active Screen')),
    );
  }
}
