import 'package:flutter/material.dart';

class AgentPendingScreen extends StatelessWidget {
  const AgentPendingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Agent Pending Screen')),
    );
  }
}
