import 'package:flutter/material.dart';

class UploadReferralScreen extends StatelessWidget {
  const UploadReferralScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Upload Referral')),
    );
  }
}
