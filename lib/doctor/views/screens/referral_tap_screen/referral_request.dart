import 'package:flutter/material.dart';

class ReferralRequestScreen extends StatelessWidget {
  const ReferralRequestScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('All Request Referral')),);
  }
}
