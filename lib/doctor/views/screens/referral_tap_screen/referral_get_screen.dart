import 'package:flutter/material.dart';

class ReferralGetScreen extends StatelessWidget {
  const ReferralGetScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Show referral that refer to current doctor')),);
  }
}
